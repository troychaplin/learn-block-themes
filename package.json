{
  "name": "learn-block-themes",
  "version": "0.0.1",
  "description": "This block theme is a canvas for learning and testing block theme concepts.",
  "author": "Troy Chaplin",
  "license": "GPL-2.0-or-later",
  "main": "index.js",
  "scripts": {
    "start": "wp-scripts start --webpack-src-dir=assets --output-path=build",
    "build": "wp-scripts build --webpack-src-dir=assets --output-path=build"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.4.0",
    "path": "^0.12.7",
    "webpack-remove-empty-scripts": "^1.0.4"
  }
}
